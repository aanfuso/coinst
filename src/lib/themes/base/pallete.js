const primary = {
  main: 'hsl(360, 100%, 69%)',
};

const secondary = {
  main: 'hsl(256, 49%, 67%)',
};

const accent = {
  main: 'hsl(208, 18%, 16%)',
};

export const palette = {
  mode: 'dark',
  primary,
  secondary,
  accent,
};
